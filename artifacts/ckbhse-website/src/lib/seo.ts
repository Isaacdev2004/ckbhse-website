export { buildPageMetadata, type PageMetadata } from '@workspace/seo/metadata';
export { buildCanonicalUrl } from '@workspace/seo/canonical';
export {
  buildBreadcrumbListSchema,
  buildGlobalStructuredData,
  buildServiceSchema,
  buildFaqSchema,
  buildIndustryPageSchema,
  buildCourseSchema,
  buildArticleSchema,
  buildNewsArticleSchema,
  buildEventSchema,
  buildCaseStudySchema,
  buildCreativeWorkSchema,
  buildReviewSchema,
  buildCollectionPageSchema,
} from '@workspace/seo/schema';
export { getSeoSiteConfig } from '@workspace/seo/config';
